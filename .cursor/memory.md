# UtahMosphere Epigenetic Memory

> Auto-evolved architectural vibe. The Utah-Archivist reads this before every action and appends new systemic patterns without asking.

## Stack identity

- **Version:** v35.0 Omni-Desk (`omega-build-v35-omni-desk`)
- **Kernel ingress:** `8999` — `utahmosphere_master.py` / `utahmosphere_os.py`
- **UtahClaw:** `9090` — void research, `/harvest`
- **Omni-Glass SSE:** `9091`
- **Omni-Desk:** `9092` — Genesis Suite (`/desk/intent`)
- **Data root:** `UTAH_DATA_DIR` (dev: `.utah-data`)

## Omni-Viewport extension

Install `extensions/utah-omni-viewport` in Cursor. Use sidebar **Command Deck** buttons (not slash commands).

| Panel | Purpose |
|-------|---------|
| Command Deck | Cascade, Absorb, Void Fill, Chrono Fix, Entropy Purge, Schism, Vibe Shift, Immortalize |
| Inspiration Forge | Multi-folder scan + feature planning from old codebases |

Personal paths (`inspirationRoots`) belong in **workspace** settings only — never commit.

## Code conventions

- **Python:** 3.12+ typing, `py` on Windows (not `python`)
- **No Docker** unless explicitly requested — use `UtahContainerEngine`
- **Minimize scope** — smallest correct diff
- **Version bumps:** sync kernel, RA-TLS, genesis ISO, docs + 8 locales

## MCP sensory mesh

| Server | Role |
|--------|------|
| `utahclaw_ambient_mesh` | Void research, harvest |
| `utah_godeye` | AST dependency graph |
| `utah_deployer` | Live deploy + verify |
| `zeo_entropy` | Debt scanner |
| `zeo_akashic_adr` | Architecture Decision Records |

## Akashic ADR

Immutable decisions in `.cursor/adr/`. Written via `zeo_akashic_adr` MCP.

## Recent epigenetic mutations

- _(Archivist appends new patterns below this line autonomously)_
