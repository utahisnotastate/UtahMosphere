# Voice Command Client Template

Send voice intents without a microphone.

```bash
python command_client.py "deploy application shop"
python command_client.py "status grid"
```

After claiming your node, set `acoustic_hash` to your vibe-print.
